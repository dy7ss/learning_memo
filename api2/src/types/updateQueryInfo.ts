export interface updateQueryInfo {
    key: {
        memo_id: number
    }
    data: {
        category: string,
        subject_name: string,
        used_time: number,
        study_date: Date
    }
}
